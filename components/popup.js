import { PopupButton } from "@typeform/embed-react";

const Popup = () => {
  return (
    <PopupButton id="uCWQq4Vg" style={{ fontSize: 20 }} className="my-button">
      click to open form in popup
    </PopupButton>
  );
};

export default Popup;
